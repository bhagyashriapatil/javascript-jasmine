// spyOn: onther way to creating jasmine spy
// Global function - not on the jasmine fn
// basic usage - spyOn(Object, "methodName")



describe("In Spies Test", function(){
     var myObj = spiesTest();
    // spyOn(myObj, 'spiesTest');

    // it("return the if block executed", function(){
    //     expect(spiesTest.ifExecute()).toHaveBeenCalled();
    // });

    // it("return the else block executed", function () {
    //     expect(spiesTest.elseExecute()).toHaveBeenCalled();
    // });

})