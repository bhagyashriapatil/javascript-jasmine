// Let us turn both the variables, name and name1 as String type variables and run the same SpecRunner.html again.
//  Now check the output. It will prove that toBe() not only matches with the equivalency of the variable,
//  but it also matches with the data type or object type of the variable

describe("Different Methods of Expect Block",function (){  
    it("The Example of toBe() method",function (){ 
    //    expect(expectexam.name).toEqual(expectexam.name1);     
    });   
})
