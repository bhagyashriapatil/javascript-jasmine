describe("test for toBeTruthy()", function(){

    it("in exam object", function(){
        // expect(exam.exampleoftrueFalse(15)).toBeTruthy(false);   //Expected false to be truthy false.
    })

    it("in exam object", function(){
        expect(exam.exampleoftrueFalse(5)).toBeTruthy();   //in exam object
    })

})